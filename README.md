## Naprocademy Django 2022
### Pizza Ordering Practice

## Run
```sh
git clone [this repo]
cd pizza_ordering
```

Create a `.env` file (see `.example.env` file), and run this command:
```sh
python manage.py runserver
```